function setSelectedMenu(pageKey){
	$("#menu li").each(function(i){
		if($(this).attr("pageKey")==pageKey){
			$(this).addClass("selected");
		}
	});
}
function makeItHtmlEditor(textAreaId){
	CKEDITOR.replace(textAreaId, {
		toolbar : 'Full'
	});
}
